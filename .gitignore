node_modules
dist
dist-electron
release
*.log
.DS_Store
.vscode
.idea
*.local
.env
.env.*
!.env.example
.github/workflows/*.local.yml

# Dev-only secrets (never publish)
admin.local.json
**/eg-launcher-github-token.txt
**/eg-launcher-admin-password.txt
resources/eg-gate/*.secret
**/eg-gate.secret

# Local pack/forge test scratch
tmp-bees*
tmp-forge*

# Self-signed code signing (A1) — private key never in git
certs/
*.pfx
*.p12

# Server-side CMS secrets (upload to host only — never in the launcher)
cms-api/config.php

# Local deploy credentials / one-off ops tooling (never publish)
.deploy-secrets.json
deploy-cms-images/
scripts/_*.mjs

# Local ops notes
sql/**/*.md
cms-api/**/*.md

# Generated mail DNS / DKIM private material (never publish)
docs/mail-dns/*.pem
docs/mail-dns/dkim_private*

# DKIM private keys (server only)
cms-api/keys/


# Public website server config
website/config.php

